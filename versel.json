{
    "builds": [{ "src": "htmlCodes/**/*", "use": "@now/static" }],
    "routes": [{ "src": "/(.*)", "dest": "/htmlCodes/$1" }],
    "rewrites": [
      { "source": "/(.*)", "destination": "/" }
    ]
  }
  